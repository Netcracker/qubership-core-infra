name: Build Docker with Matrix Strategy

on:
  workflow_call:
    inputs:
      tags:
        required: false
        type: string
        default: ''
        description: 'Tags'
      dry-run:
        required: true
        type: boolean
        description: 'Dry run'
      config-filename:
        required: false
        type: string
        default: 'docker-dev-config.json'
        description: 'Set name only or short path inside .github folder if required'
      ref:
        required: false
        type: string
        description: 'Git reference to use for the build'
      build-args:
        description: 'Build arguments for the Docker image, in key=value format, one per line.'
        required: false
        type: string
        default: ''

jobs:
  dry-run:
    name: Build Docker images without publishing
    if: ${{ inputs.dry-run == true }}
    permissions:
      contents: read
    uses: $/.github/workflows/docker-build-core.yaml
    with:
      tags: ${{ inputs.tags }}
      dry-run: true
      config-filename: ${{ inputs.config-filename }}
      ref: ${{ inputs.ref }}
      build-args: ${{ inputs.build-args }}

  # Publication inherits its caller's ceiling; requesting write here would invalidate read-only dry-run callers.
  publish: # zizmor: ignore[excessive-permissions]
    name: Build Docker images
    if: ${{ inputs.dry-run == false }}
    uses: $/.github/workflows/docker-build-core.yaml
    with:
      tags: ${{ inputs.tags }}
      dry-run: false
      config-filename: ${{ inputs.config-filename }}
      ref: ${{ inputs.ref }}
      build-args: ${{ inputs.build-args }}
