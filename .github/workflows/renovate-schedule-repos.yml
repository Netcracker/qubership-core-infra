name: renovate schedule for repositories

on:
  schedule:
    - cron: '15,45 * * * *'

jobs:
  call:
    uses: ./.github/workflows/renovate.yml
    with:
      type: "repositories"
      dry-run: 'false'
      log-level: 'info'
